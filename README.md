# Grub-Dash-Express-Server

This is a Node.js Express API used to serve orders and meals to a restaurants ordering system.

This was an excerise in Express, so it doesn't have any front-end or database. 
